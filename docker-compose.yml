version: "3.8"

services:
  cv:
    build:
      context: ./
      dockerfile: dockerfile
    container_name: cv
    volumes:
      - ./:/app
    ports:
      - 2173:2173
      - 3173:3173
    tty: true
