version: "3.8"

services:
  cv:
    build:
      context: ./
      dockerfile: dockerfile
    container_name: cv
    volumes:
      - ./:/app
    ports:
      - 5173:5173
      - 4173:4173
    tty: true
